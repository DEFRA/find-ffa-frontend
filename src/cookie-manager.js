/* eslint-disable @typescript-eslint/no-unused-vars */
// Refer to ADP repo for original cookie-manager - wasn't ported as we're not doing login at the moment

function isAuthenticated(_request, _h) {
  return true
}

export { isAuthenticated }
